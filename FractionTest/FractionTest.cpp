#include "pch.h"
#include "CppUnitTest.h"

#include "../Calcul/fraction.h"
#include "../Calcul/trans.h"

using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace FractionTest
{
	TEST_CLASS(FractionTest)
	{
	public:
		/*
		TEST_METHOD(TestMethod1)
		{
			Assert::AreEqual(std::string("1"), (Fraction(1, 2) + Fraction(1, 2)).toString());
			Assert::AreEqual(std::string("2'1/7"), (Fraction(7, 7) + Fraction(8, 7)).toString());
			Assert::AreEqual(std::string("0"), (Fraction(1, 2) - Fraction(1, 2)).toString());
			Assert::AreEqual(std::string("1/4"), (Fraction(1, 2) * Fraction(1, 2)).toString());
			Assert::AreEqual(std::string("1"), (Fraction(1, 2) / Fraction(1, 2)).toString());
			Assert::AreEqual(std::string("1/6"), (Fraction(1, 2) * Fraction(2, 6)).toString());
		}

		TEST_METHOD(TestMethod2)
		{
			Assert::AreEqual(0, cstrToInt(""));
			Assert::AreEqual(4396, cstrToInt("4396"));
			Assert::AreEqual(777, cstrToInt("777"));
			Assert::AreEqual(0, cstrToInt(nullptr));
		}

		TEST_METHOD(TestMethod3)
		{
			Assert::AreEqual(std::string("1/4"), Fraction("1/4").toString());
			Assert::AreEqual(std::string("4396"), Fraction("4396").toString());
			Assert::AreEqual(std::string("7'6/7"), Fraction("7'6/7").toString());
		}*/

	};
}
